const cart = document.getElementById('cart');
let cartContagen = 0;
button. addEventListener ('click' , () => {
    const nome = document.getElementById('nome').value;
    const numero = document.getElementById('numero').value;
    if (nome && numero) {

        
    } else{
        alert('escrivi na tudu campos .');
    }

});

<button onclick="documento.GETELEMENTBYID ('demo').innnerhtml=DATE()">clica aqui</button>
button.addEventListener('click', () =>{
    if (button = onclick Event) {
        ultimo-pedido.document.getElementById(pedido, )
    }
})





b
}
construacao de um aplicativo vai ser mas facil do que penca vamos fazzer mas facil para incerir as imagem asim que concluimos vamos te informar  